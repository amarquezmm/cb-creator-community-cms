'use strict';

/**
 * become-a-creator controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::become-a-creator.become-a-creator');
